import React, {PropTypes} from 'react';

const Quote = ({ onClick, like, text }) => (
    <li>
        onClick={onClick}
    </li>
)

$NAME.propTypes = {
    //loading: PropTypes.bool.isRequired
};

export default Quote;
